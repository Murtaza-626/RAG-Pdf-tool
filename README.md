# RAG-Pdf-tool
A Python-based application that lets users ask questions or summarize content from PDF documents using local Ollama model. It uses a retrieval-augmented generation (RAG) pipeline with local LLMs, enabling fast, accurate, and private responses without sending data to external APIs.
